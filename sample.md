# 面试录音整理（测试样本）

面试官：你好，我们先聊聊你最近做的项目吧，挑一个你最有成就感的。

候选人：好的。我上一家公司做的最后一个是 AI 辅助写作的产品，我在里面负责前端。这个项目大概做了半年，主要是把原来的在线编辑器接入了大模型能力，用户可以选中一段文字让 AI 改写、润色、续写。技术上我是用 React 做的，跟后端联调走的是 SSE 流式返回。上线之后这个功能的日活渗透率大概到了 35%。

面试官：你们是怎么处理流式渲染的性能问题的？

候选人：我们就是每次收到 chunk 就 setState，把新的内容拼到后面。性能上没遇到什么问题，因为一次返回的文本也不会特别长。

面试官：那如果用户在一次流式生成的过程中连续输入，或者频繁切换选区，会有问题吗？

候选人：这个……应该没什么问题吧，React 会自己处理 diff，只更新变化的部分。真出问题的话加个 debounce 应该就行了。

面试官：好，我们写点代码。给你一个数组，找出其中和为 target 的两个数的下标，返回一组即可。

候选人：嗯，暴力解法是双层循环，时间复杂度 O(n²)。优化的话用哈希表，遍历的时候判断 target 减当前值在不在 map 里，在的话就返回，时间复杂度 O(n)，空间 O(n)。我写一下。……（写代码）好，大概是这样的。

面试官：如果有多个答案，或者没有答案呢？

候选人：没有答案的话返回空数组，多个答案的话我现在的实现只返回第一组。

面试官：React 里 key 的作用是什么？为什么不能用 index？

候选人：key 是给 React 用的，帮助它识别哪些元素变了，这样能提升性能。用 index 的话……应该也可以吧，只是不推荐，React 会警告。

面试官：那如果列表会插入或删除元素，用 index 会怎样？

候选人：嗯……我不太确定，可能会重新渲染吧。

面试官：我们聊聊系统设计。假设要设计一个短链服务，你会怎么设计？

候选人：短链的话，就是存一个映射，长链接对应一个短的 key。生成 key 可以用哈希或者自增转 base62。数据库存映射关系，访问的时候查一下然后 302 跳转。大概就是这样。

面试官：存储和并发量上有什么考虑吗？比如每天生成一亿条，怎么保证 key 不冲突？

候选人：一亿的话……可以用 Redis 做缓存，数据库分库分表。key 冲突的话可以用分布式 ID 生成器，比如雪花算法，保证唯一。

面试官：那如果同一个长链接被多次提交，你希望返回同一个短链还是不同的？

候选人：应该是不同的吧，每次请求都生成一个新的。

面试官：最后一个问题，你平时的学习方式是什么样的？

候选人：我一般会看官方文档，遇到问题先搜一下，也会看一些技术博客和开源项目源码。

# 面试复盘

### 1. 问题列表

1. 介绍最近最有成就感的项目，以及你的职责、技术链路和结果。
2. 如何处理 AI 流式渲染的性能问题？
3. 流式生成期间用户连续输入或频繁切换选区，会有什么问题？
4. 找出数组中和为 `target` 的两个数的下标。
5. 如果有多个答案，或者没有答案，应该怎么处理？
6. React 中 `key` 的作用是什么，为什么通常不能用 `index`？
7. 列表插入或删除元素时，使用 `index` 会怎样？
8. 如何设计一个短链服务？
9. 每天生成一亿条短链时，存储、并发和 key 冲突怎么处理？
10. 同一个长链接多次提交，返回相同还是不同的短链？
11. 你平时的学习方式是什么？

算法题的解题思路正确，但转录中没有实际代码，因此无法判断最终实现是否完全正确。

### 2. 问题深意

#### 问题 4-5：Two Sum 与边界条件

- **考察目的**：是否掌握哈希表解题，并能主动处理无解、多解、重复元素等边界情况。
- **深层意图**：不只看能否写出模板，更看你是否先确认答案唯一性、返回全部还是任意一组、数组是否可修改。
- **理想回答方向**：说明哈希表保存 `值 -> 下标`，遍历时先查询再写入；补充 `O(n)` 时间、`O(n)` 空间，并明确无解返回 `[]`、有多个答案时返回任意一组还是全部。

#### 问题 2-3：流式渲染与交互并发

- **考察目的**：是否真正处理过 SSE 流式前端，而不是只完成了“收到 chunk 后显示文本”。
- **深层意图**：高频更新是否引起性能问题、旧请求是否会污染新内容、选区变化后 AI 结果应该写到哪里。
- **理想回答方向**：区分网络接收、文本缓冲和 UI commit；使用批量提交、增量解析、组件隔离；通过 `requestId`、`AbortController` 和选区快照处理取消、竞态与陈旧响应。`debounce` 不能代替竞态控制。

#### 问题 6-7：React key 与状态身份

- **考察目的**：理解 reconciliation，以及列表增删时组件状态、DOM 状态和渲染结果如何关联。
- **深层意图**：判断你是否知道 `key` 是元素身份标识，而不只是“帮助性能”。
- **理想回答方向**：稳定唯一的 `key` 能保证 React 将旧节点和正确的新节点匹配；用 `index` 时，插入或删除会让后续元素身份整体平移，可能复用错误的组件状态、输入框内容和动画状态。静态且永不重排的列表才通常可以接受 `index`。

#### 问题 8-10：短链系统设计

- **考察目的**：能否从需求澄清推进到生成、存储、访问、扩展和一致性设计。
- **深层意图**：检查你是否会先确认规模、保留期、自定义别名、分析能力和重复 URL 语义，而不是直接给出一个映射表。
- **理想回答方向**：先估算读写 QPS、存储量和峰值；设计 API、Base62 key、数据库唯一约束、缓存、分片、限流和监控；深入讨论全局 ID、冲突重试、缓存一致性、`301/302` 的取舍，以及重复长链接的幂等策略。

#### 问题 1、11：经历可信度与学习能力

- **考察目的**：项目是否真实、职责是否清晰，以及遇到未知问题时是否有稳定的学习闭环。
- **深层意图**：验证结果指标背后的技术贡献，并判断你是否具备主动深挖和持续成长的能力。
- **理想回答方向**：项目按“背景、职责、架构链路、难点、结果、复盘”表达；学习方式给出具体案例，例如通过官方文档、源码、最小实验和输出笔记解决过什么问题。

### 3. 面试结果分析

- **总体评价**：候选人能给出正确的算法方向，也能描述短链的基本架构，但多处回答停留在“应该可以”“React 会处理”等表层判断，尚未证明实际处理过高频流式和复杂状态竞态。
- **正确率**：完全正确 2/11，约 18%；正确或基本可接受 5/11，约 45%；部分掌握 3 题；明显错误或不知道 3 题。
- **技术深度**：5/10。知道哈希表、SSE、Base62 和雪花算法，但缺少机制、失败模式和工程细节。
- **问题解决能力**：4/10。算法题表现最好；系统和前端问题较少主动确认约束。
- **系统设计能力**：5/10。基础链路正确，但规模、冲突、幂等、缓存一致性和容灾讨论不足。
- **沟通表达**：5/10。回答简洁，但结论缺少证据；多次以不确定语气结束关键回答。
- **主要优势**：算法基础方向正确；项目结果有量化指标；能快速提出 Redis、分库分表和分布式 ID 等常见组件。
- **主要短板**：React 状态身份与协调机制不牢；流式请求的取消和竞态处理不足；系统设计缺少需求澄清和取舍分析。

### 4. 错误答案纠正

#### 问题 2：如何处理流式渲染性能问题？

- **你的回答**：每收到 chunk 就 `setState` 拼接；文本不长，所以没有性能问题。
- **问题所在**：连续 chunk 可能触发多次渲染、Markdown 重新解析和整段文本重排。即使单次文本不长，生成较快时仍可能产生掉帧，并且没有讨论取消、缓冲区或渲染分层。
- **正确答案**：将网络 chunk 放入缓冲区，按 `requestAnimationFrame` 或固定时间窗口批量提交；流式区域与编辑器其他状态隔离；对已完成段落使用 memo 或增量解析；长文档只渲染可见区域；同时保留取消和背压能力。
- **建议**：回答性能题时至少说明“瓶颈在哪里、如何测量、采用什么机制、如何降级”。

#### 问题 3：用户连续输入或切换选区怎么办？

- **你的回答**：React 会处理 diff，真有问题就加 `debounce`。
- **问题所在**：这里的主要问题不是 diff，而是请求竞态、过期 chunk 和编辑器选区映射。旧请求可能继续写入新位置，用户输入也可能使原选区失效。
- **正确答案**：每个生成请求携带唯一 `requestId` 并保存开始时的文档版本及选区快照；新请求使用 `AbortController` 取消旧请求；接收 chunk 时校验请求仍有效；选区变化时选择取消、重定向到原目标或根据事务映射调整范围。
- **建议**：看到“连续操作、频繁切换、旧请求”等信号，优先想竞态和状态一致性，而不是单纯防抖。

#### 问题 6：React key 的作用是什么？

- **你的回答**：key 主要提升性能；使用 `index` 应该也可以，只是 React 会警告。
- **问题所在**：`key` 的核心是元素身份，不只是性能优化；React 通常警告的是列表缺少 key，而不是使用 `index` 就必然警告。
- **正确答案**：`key` 告诉 React 新旧节点之间的对应关系，用于决定复用、移动、创建或销毁。稳定 key 能保留正确的组件状态和 DOM 状态；`index` 在动态列表中会改变身份语义。
- **建议**：优先使用业务唯一 ID；只有静态、不重排的列表才可以考虑 `index`。

#### 问题 7：插入或删除元素时，使用 index 会怎样？

- **你的回答**：不确定，可能会重新渲染。
- **问题所在**：可能不只是重新渲染，而是把原有组件状态关联到错误的列表项。例如输入框内容、焦点、展开状态和动画可能跟随错误元素。
- **正确答案**：删除首项后，后续元素的 index 全部变化，React 可能把原第 2 项视为新的第 1 项并复用其状态。应为每项提供稳定唯一 key；若没有天然 ID，可建立稳定客户端 ID，但不能每次渲染生成随机 key。
- **建议**：把 key 理解为“元素在多次渲染之间的身份证”，而不是排序编号。

#### 问题 9：每天一亿条时如何保证 key 不冲突？

- **你的回答**：Redis 缓存、分库分表，再用雪花算法保证唯一。
- **问题所在**：方向不错，但 Redis 不负责生成唯一业务 key，也不应默认作为唯一数据源；雪花算法需要解决 worker ID、时钟回拨和编码长度问题，而且完整 64 位 ID 经 Base62 后通常不是最短。
- **正确答案**：一亿/天平均约 `1157 QPS`，峰值可能是数倍。可使用全局计数器服务分段预分配号段，再由各节点本地生成 Base62 key；或使用雪花算法并保证实例 ID 唯一。数据库建立唯一索引作为最终防线，冲突时重试；按 key 哈希分片，Redis 只缓存热点映射，并设计空值缓存和过期策略。
- **建议**：系统设计中使用分布式 ID 时，要同时说明唯一性来源、时钟问题、编码长度、故障恢复和数据库兜底。

#### 问题 11：你平时的学习方式是什么？

- **你的回答**：看官方文档、搜索、技术博客和开源项目源码。
- **问题所在**：内容泛化，没有证明深度和持续性，也没有说明如何验证自己真正学会。
- **正确答案**：给出一个真实案例，例如“为解决 SSE 断线重连，我先查标准文档，再阅读浏览器事件模型和开源客户端实现，写最小复现实验，最后形成团队文档”。这种回答同时体现输入、实践、验证和输出。
- **建议**：准备一个 60 秒的学习案例，重点讲问题、信息源、验证方式和最终产出。

### 5. 面试总结

#### 1. 建议你重点补的内容

- **React 渲染机制**：`key`、reconciliation、状态身份、组件复用、受控组件、渲染隔离和性能测量。
- **流式前端架构**：SSE 解析、chunk 缓冲、批量提交、增量 Markdown、取消请求、竞态控制、编辑器选区映射。
- **系统设计基础**：需求澄清、QPS/存储估算、Base62、全局 ID、号段分配、唯一约束、分片、缓存一致性和幂等。
- **项目表达**：每个项目准备“业务背景 → 我的职责 → 架构链路 → 技术难点 → 结果指标 → 复盘”的两分钟答案。
- **算法表达**：写题前明确输入输出、重复元素、无解和多解要求；编码后说明复杂度并主动跑边界用例。

#### 2. 综合建议

1. 建好技术叙事主线。把 AI 写作项目讲成一条完整链路，而不是只报出 React、SSE 和 35% 渗透率。
2. 不确定时不要只说“应该可以”。先说明假设，再给出机制、风险和验证方式，例如“如果 chunk 频率高，我会先用 Performance 面板确认渲染瓶颈，再决定是否批处理”。
3. 系统设计先问再答。短链题目至少确认读写规模、保留时间、自定义别名、分析需求和重复 URL 是否幂等。
4. 准备具体的逆向提问，例如短链系统的数据规模、团队如何处理缓存一致性和线上故障，以及该岗位更看重前端性能还是全栈能力。